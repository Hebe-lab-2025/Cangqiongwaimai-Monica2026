这个错误 **和 Lombok 已经没关系了**，是一个**模块 / 依赖 / 包路径问题**。

```
java: package com.sky.dto does not exist
```

👉 **编译器在 classpath 里，根本没找到 `com.sky.dto` 这个包。**

下面按「最常见 → 最容易忽略」给你精准排查。

---

## 一句话定位（先记住）

> **`com.sky.dto` 不在 `sky-server` 里，而是在别的模块（99% 是 `sky-pojo`）**
>
> 👉 要么 **模块没被正确引入**
> 👉 要么 **代码不在标准 Maven 目录**
> 👉 要么 **IDEA 没刷新依赖**

---

## Step 1️⃣：确认 `com.sky.dto` 在哪一个模块（关键）

在项目根目录执行：

```bash
grep -R "package com.sky.dto" -n .
```

你会看到类似：

```text
sky-pojo/src/main/java/com/sky/dto/EmployeeLoginDTO.java
```

✅ 这一步的结论非常重要：

| 情况             | 含义                |
| -------------- | ----------------- |
| 在 `sky-pojo`   | ✅ 正常（最常见）         |
| 在 `sky-common` | 也可以               |
| 根本搜不到          | ❌ DTO 文件没了 / 包名写错 |

---

## Step 2️⃣：如果 DTO 在 `sky-pojo`（你现在就是这个情况）

你 **sky-server 的 pom 已经写了**：

```xml
<dependency>
    <groupId>com.sky</groupId>
    <artifactId>sky-pojo</artifactId>
    <version>1.0-SNAPSHOT</version>
</dependency>
```

但还是报错 → **说明 sky-pojo 模块本身有问题**

### 🔥 重点检查 3 件事

#### ✅ 1. sky-pojo 是不是 Maven 模块（不是普通文件夹）

打开：

```
sky-pojo/pom.xml
```

必须有：

```xml
<artifactId>sky-pojo</artifactId>
<packaging>jar</packaging>  <!-- 默认就是 jar -->
```

如果：

* 没有 `pom.xml`
* 或 packaging=pom

❌ 那这个模块 **不会产出 jar**，server 用不了

---

#### ✅ 2. DTO 是否在 **标准 Maven 路径**

**必须是：**

```
sky-pojo
└── src
    └── main
        └── java
            └── com
                └── sky
                    └── dto
                        └── XxxDTO.java
```

❌ 下面这些都会导致 **package 不存在**：

```
src/java/...
src/main/com/sky/dto
src/main/resources/com/sky/dto
```

---

#### ✅ 3. DTO 文件头部 package 是否真的写对

打开任意 DTO：

```java
package com.sky.dto;
```

❌ 常见低级错误（IDE 不报红，但编译失败）：

```java
package com.sky.DTO;   // 大小写不同
package com.sky.dtos; // 多了 s
```

⚠️ **Java 包名是大小写敏感的**

---

## Step 3️⃣：如果一切都对，但 IDEA 还报错（非常常见）

这是 **IDE 缓存问题**，不是代码问题。

### 解决方式（按顺序）

#### ① Maven 重新加载

* 打开 Maven 面板
* 点击 🔄 Reload All Maven Projects

#### ② 如果还不行（必杀技）

```
File → Invalidate Caches / Restart
→ Invalidate and Restart
```

---

## Step 4️⃣：终极验证（不用 IDEA）

在项目根目录跑：

```bash
mvn clean install -DskipTests
```

### 结果判断

| 结果               | 说明                          |
| ---------------- | --------------------------- |
| ✅ BUILD SUCCESS  | IDEA 的问题                    |
| ❌ 还是 package 不存在 | 一定是 **目录 / package / 模块** 错 |

---

## ✅ 正确架构（对照你项目）

```
sky-take-out-Monica (parent)
├── sky-server
│   └── src/main/java/com/sky/controller/...
│   └── pom.xml
├── sky-pojo
│   └── src/main/java/com/sky/dto/...
│   └── pom.xml
├── sky-common
│   └── src/main/java/com/sky/utils/...
```

server **只 import，不定义 DTO**

---

## 如果你想一步到位

你可以直接贴：

1️⃣ `sky-pojo` 的目录截图（或文字结构）
2️⃣ 任意一个 `DTO` 的 **完整 package + 文件路径**
3️⃣ 报错的 `import com.sky.dto.xxx;` 那一行

我可以 **10 秒内帮你精确指出是哪一步错了**。
