```
Error starting ApplicationContext. To display the conditions report re-run your application with 'debug' enabled.
2026-01-06 20:49:32.273 ERROR 63457 --- [           main] o.s.b.d.LoggingFailureAnalysisReporter   : 

***************************
APPLICATION FAILED TO START
***************************

Description:

Web server failed to start. Port 8080 was already in use.

Action:

Identify and stop the process that's listening on port 8080 or configure this application to listen on another port.


Process finished with exit code 1

```
————————
```
yonggan@Yonggans-MacBook-Pro sky-take-out-Monica % lsof -i:8080
COMMAND     PID    USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
VooV\x20M   679 yonggan  163u  IPv4 0x81abbee909650eda      0t0  TCP 10.0.0.90:50491->43.130.30.145:http-alt (ESTABLISHED)
java      58659 yonggan  170u  IPv6 0xbb0aba8698ee7e3e      0t0  TCP *:http-alt (LISTEN)
yonggan@Yonggans-MacBook-Pro sky-take-out-Monica %
```

好，这一段输出我们一句一句拆开看 👇

```bash
yonggan@Yonggans-MacBook-Pro sky-take-out-Monica % lsof -i:8080
COMMAND     PID    USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
VooV\x20M   679 yonggan  163u  IPv4 0x81abbee909650eda      0t0  TCP 10.0.0.90:50491->43.130.30.145:http-alt (ESTABLISHED)
java      58659 yonggan  170u  IPv6 0xbb0aba8698ee7e3e      0t0  TCP *:http-alt (LISTEN)
```

## 1️⃣ 这条命令在干嘛？

```bash
lsof -i:8080
```

* `lsof`：**list open files** 的缩写

  * 在 Unix 里，“一切皆文件”，包括**网络端口、socket** 等
  * 所以 `lsof` 可以用来查“谁在用某个端口”
* `-i:8080`：表示**查所有跟端口 8080 相关的网络连接**

也就是：
👉「列出所有正在使用 **端口 8080** 的进程」

---

## 2️⃣ 表头每一列是什么意思？

这一行是表头：

```text
COMMAND     PID    USER   FD   TYPE   DEVICE SIZE/OFF NODE NAME
```

* **COMMAND**：进程名（哪个程序）
* **PID**：进程 ID（后面 kill 要用）
* **USER**：哪个用户启动的（你这里是 `yonggan`）
* **FD**：file descriptor（文件描述符），简单理解就是“这个进程打开的第几个文件/连接”
* **TYPE**：连接类型

  * IPv4 / IPv6
* **DEVICE / SIZE/OFF / NODE**：底层设备、偏移等，一般不必关心
* **NAME**：最关键，显示：

  * 本机 IP 和端口
  * 远端 IP 和端口
  * 连接状态（LISTEN、ESTABLISHED 等）

---

## 3️⃣ 第一行：VooV 这行在干嘛？

```text
VooV\x20M   679 yonggan  163u  IPv4 0x81abbee909650eda      0t0  TCP 10.0.0.90:50491->43.130.30.145:http-alt (ESTABLISHED)
```

逐块解释：

* **COMMAND = `VooV\x20M`**

  * 这是腾讯会议国际版（VooV Meeting）之类的进程名
  * `\x20` 就是空格（space）的转义
* **PID = 679**

  * 进程 ID 679
* **TYPE = IPv4**

  * 使用 IPv4 协议
* **NAME = `10.0.0.90:50491->43.130.30.145:http-alt (ESTABLISHED)`**

再拆：

* `10.0.0.90:50491`

  * 本机 IP 和端口：你电脑的某个临时高位端口 **50491**
* `->`

  * 表示这是一个**已经建立的连接**
* `43.130.30.145:http-alt`

  * 远端服务器 IP
  * `http-alt` = 端口 8080 的服务名（8080 对应的“别名”）
* `(ESTABLISHED)`

  * 表示这个连接已经**建立成功、正在通信**

👉 这行表示：
**VooV Meeting 正在通过你本机临时端口 50491，连到远程服务器的 8080 端口。**
它只是**作为客户端在访问别人家的 8080**，**不是在本机监听 8080**，所以**跟 Spring Boot 冲突无关**。

---

## 4️⃣ 第二行：真正抢占 8080 的是这个 `java`

```text
java      58659 yonggan  170u  IPv6 0xbb0aba8698ee7e3e      0t0  TCP *:http-alt (LISTEN)
```

关键字段：

* **COMMAND = `java`**

  * 说明这是一个 Java 程序
  * 很可能是你之前启动的某个 Spring Boot 项目 / 其他 Java 服务，没有正常停掉
* **PID = 58659**

  * 进程 ID，后面 kill 要用的就是它
* **TYPE = IPv6**

  * 使用 IPv6（没关系，照样会占用 8080）
* **NAME = `*:http-alt (LISTEN)`**

再拆：

* `*`：表示监听所有地址（0.0.0.0 或 ::）
* `http-alt`：端口别名，对应 **8080**
* `(LISTEN)`：**监听状态**，表示这个进程在本机“占着”端口 8080 等别人来连

👉 所以这行的含义是：

> 有一个 **Java 程序（PID 58659）正在本机监听 8080 端口**，
> 这就是你新项目启动时报错 “Port 8080 was already in use” 的直接原因。

---

## 5️⃣ 接下来你应该怎么做？

### ✅ 方案 1：杀掉这个旧的 Java 进程

在终端执行：

```bash
kill 58659
```

如果它比较顽固，换成强制：

```bash
kill -9 58659
```

然后再验证一次：

```bash
lsof -i:8080
```

✅ 若没有任何输出，说明 8080 已经空出来了
👉 此时重新 Run 你的 Spring Boot 项目，就能正常在 8080 启动。

---

### ✅ 方案 2：不杀进程，改你当前项目的端口

如果你不想关掉旧的 Java 程序（比如是别的服务），可以：

在 `application.yml` 里加：

```yaml
server:
  port: 8081
```

或者 `application.properties`：

```properties
server.port=8081
```

改成 8081 / 9000 等任意没冲突的端口重新启动。

---

## 6️⃣ 再顺一遍你现在的情况（帮你总结）

* 你执行 `lsof -i:8080`，看到两行：

  * **VooV Meeting：只是作为客户端连别人的 8080，不占你本机 8080**
  * **java PID 58659：在本机监听 8080，是导致冲突的罪魁祸首**
* Spring Boot 报错：

> Web server failed to start. Port 8080 was already in use.

就是因为这个 **java 58659** 已经先启动过一个服务监听了 8080。

---

如果你愿意，你可以把：

```bash
lsof -i:8080
```

在你 `kill` 之后的最新输出发给我，我可以帮你确认：

* ✅ 8080 是否真的空出来了
* ✅ 哪个 Spring Boot 项目在占端口
* ✅ 顺带帮你用“宿舍房间被占”的比喻，记一个口语化解释，面试时可以说清楚什么是“端口被占用、如何排查” 🧠


很好，你这一步已经操作对了 ✅，我们来读一下现在的结果：

```bash
yonggan@Yonggans-MacBook-Pro sky-take-out-Monica % lsof -i:8080
COMMAND   PID    USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
VooV\x20M 679 yonggan  163u  IPv4 0x81abbee909650eda      0t0  TCP 10.0.0.90:50491->43.130.30.145:http-alt (ESTABLISHED)
```

## 1️⃣ 现在 8080 端口还被占吗？

**结论：本机 8080 端口已经不被占用了，可以重新启动 Spring Boot 了 ✅**

为什么这么说？关键点有两个：

### 🔍 之前 vs 现在的对比

**之前的输出（有问题的时候）：**

```text
java      58659 yonggan  170u  IPv6 ... TCP *:http-alt (LISTEN)
```

* COMMAND = `java`
* NAME = `*:http-alt (LISTEN)`
  👉 说明：**你的电脑在“监听”8080 端口**，也就是“我开着门等别人来访问 8080”

---

**现在的输出：**

```text
VooV\x20M 679 yonggan ... TCP 10.0.0.90:50491->43.130.30.145:http-alt (ESTABLISHED)
```

* 只有一行 VooV Meeting（腾讯会议国际版）
* 关键字段是 `50491->43.130.30.145:http-alt (ESTABLISHED)`

拆一下这一段：

* `10.0.0.90:50491`
  👉 你的电脑本地端口是 **50491**（一个临时随机端口）
* `->`
  👉 正在连接到远程服务器
* `43.130.30.145:http-alt`
  👉 远端服务器的 **8080 端口**
* `(ESTABLISHED)`
  👉 说明这是一个**已经建立成功的连接**

也就是说：

> 腾讯会议只是从你电脑的 **50491 端口**，连到“别人家”的 **8080 端口**
> 它 **不是** 在你电脑上监听 8080
> 所以 **不会阻止你的 Spring Boot 占用本机 8080**

只要输出里 **没有类似 `*:http-alt (LISTEN)` 的那一行**，就说明你本机的 8080 端口已经“空出来”了。

---

## 2️⃣ 小比喻：为什么这个不会冲突？

可以这样想：

* 端口 = 房间号
* “LISTEN” = 你在 **开个门（8080 房间），等别人来找你**
* “ESTABLISHED” = 你从自己家里的一个小窗（50491）跑出去，去别人的 8080 房间聊天

现在只有：

* 你从 **50491 → 别人家的 8080** 在聊天（客户端连接）
* 你自己家 **8080 房间现在是空的**，没人开这个房间

所以你的 Spring Boot 完全可以说：

> “那我现在来占用我家 8080 房间，开始监听请求了。”

---

## 3️⃣ 你下一步可以做什么？

现在可以直接：

1. 回到 IDEA
2. 再次运行你的 Spring Boot 项目
3. 如果启动正常，你会在控制台看到类似：

```text
Tomcat started on port(s): 8080 (http)
Started XxxApplication in xx.xxx seconds
```

如果你想验证一下“谁在监听 8080”，可以：

* **项目启动后** 再执行一次：

```bash
lsof -i:8080
```

你应该会看到类似：

```text
java  PID  yonggan  ... TCP *:http-alt (LISTEN)
```

这次的 `java` 就是**你当前这个 Spring Boot 项目**，而不是之前那个“僵尸进程”。

---

## 4️⃣ 总结一下这次完整排查流程（你可以记下来，面试也能讲）

1. Spring Boot 报错：`Port 8080 was already in use`
2. 用 `lsof -i:8080` 查谁占了 8080
3. 发现有一行：`java ... *:http-alt (LISTEN)` → Java 程序在监听 8080
4. 用 `kill <PID>` 干掉该进程
5. 再 `lsof -i:8080`：

   * 只剩 `xxx:50491->xxx:http-alt (ESTABLISHED)`（客户端连接）✅
   * 没有 `*:http-alt (LISTEN)` 了 → 8080 已空
6. 重新启动 Spring Boot，启动成功 🎉

---

如果你愿意，我可以帮你把这段经历整理成一段 **“面试自我介绍 / Debug 故事”**，关键词包括：

* 如何排查端口占用
* 使用 `lsof` / `kill`
* 区分 `LISTEN` vs `ESTABLISHED`
* 怎么快速恢复服务 💻


很好，这就是一条**常用排错技能** 👍
下面给你一套标准流程：**如何排查端口占用，并用 lsof / kill 解决问题**（Mac/Linux 专用，Windows 我也可以给另一套）

---

# ✅ 目标一句话

> 找到谁占用了端口 ➜ 确认类型 ➜ 结束进程 ➜ 释放端口

---

## 🥇 第一步：查看端口谁在占用（lsof）

### 🔎 查看端口对应的进程

```bash
lsof -i:端口号
```

例如端口 8080：

```bash
lsof -i:8080
```

### 输出示例

```text
COMMAND   PID    USER   FD   TYPE  DEVICE  SIZE/OFF NODE NAME
java     58659 yonggan  170u IPv6           TCP *:http-alt (LISTEN)
```

### ⭐ 重点看 3 个字段

| 字段      | 意义                       |
| ------- | ------------------------ |
| COMMAND | 谁在占用（哪个程序）               |
| PID     | 进程号（后面 kill 要用）          |
| NAME    | 状态（LISTEN / ESTABLISHED） |

---

## 🧐 第二步：判断它是在“占用”还是“访问别人”

### ✔ 真正占用端口的是这种（监听 LISTEN）

```text
*:http-alt (LISTEN)
```

👉 **本机占着端口，阻止别人绑定**

### ❌ 这种不是占用（只是访问别人）

```text
50491->43.130.30.145:http-alt (ESTABLISHED)
```

👉 只是客户端连接，**不会导致端口冲突**

📌 口诀：

> LISTEN = 占座
> ESTABLISHED = 聊天

---

## 🛠 第三步：结束占用端口的进程（kill）

找到 PID（例如 58659）

### ⚠️ 温和结束（优先）

```bash
kill 58659
```

### 💥 强制结束（如果没反应）

```bash
kill -9 58659
```

---

## 🔁 第四步：再次确认端口是否释放

```bash
lsof -i:8080
```

👉 没有输出 = 端口已空，可以启动项目 ✅

---

# 🧭 Windows 版本（如果你以后用 Windows）

### 查端口

```bat
netstat -ano | findstr 8080
```

### 结束进程

```bat
taskkill /PID 12345 /F
```

---

# 🎯 口语答法（简短版，适合面试）

> To troubleshoot “port already in use”,
> I first run `lsof -i:<port>` to identify which process is listening on that port.
> Then I check the PID and use `kill` (or `kill -9` if needed) to terminate it.
> After confirming the port is released, I restart the application.

👉 关键词：

* troubleshoot
* listening port
* PID
* terminate process
* restart application

---

## 🚀 如果你愿意，我还可以：

🔹 画 **端口占用 ASCII 图**
🔹 给你 **IDEA 内置端口查看方法**
🔹 教你 **端口冲突排查流程图**
🔹 给你 **常见报错中文解释**：

* `Address already in use`
* `Port already in use`
* `BindException`

只要告诉我端口号或报错截图即可 💻


