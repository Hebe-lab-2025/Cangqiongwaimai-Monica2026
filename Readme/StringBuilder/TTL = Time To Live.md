**TTL** 是一个缩写，根据场景不同有两种常见含义：

---

## ✅ 在计算机/网络/缓存中（最常见）

👉 **TTL = Time To Live**

意思：**存活时间 / 过期时间**

表示数据还能存在多久，例如：

* Redis 缓存 TTL
* JWT token 过期时间
* DNS 记录生存时间
* HTTP 缓存头 `Cache-Control: max-age`
* 消息队列消息过期时间

例子：

> token TTL = 3600 秒
> 👉 表示 1 小时后自动失效

---

## 🧭 在硬件/电子领域

👉 **TTL = Transistor–Transistor Logic**

中文：**晶体管–晶体管逻辑电路**

用于描述一种数字电路技术（现在不如 CMOS 常见）

---

## 🎯 面试时一句话回答

> TTL usually means “Time To Live,” which represents how long data is valid before it expires, commonly used in caching, tokens, and DNS.

---

如果你愿意我还能帮你：

* Redis 中 TTL 作用
* JWT 中 TTL vs exp 区别
* 设置 Spring Boot Redis TTL 示例代码
* 画一张 “TTL 过期流程图”
